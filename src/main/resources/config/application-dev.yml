wechat:
  token: "0ADCEAF6CCE07DF3"
server:
  port: 8080
spring:
  #参照地址，用于给用户返回定制的url，开发环境根据本地环境修改
  url: "http://localhost:8081"
  #mysql数据库的配置
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://39.106.75.80:54306/wechatDB?useUnicode=true&autoReconnect=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: liuwr12138..who
    druid:
      db-type: com.alibaba.druid.pool.DruidDataSource
  #redis数据库的配置
  redis:
    host: 39.106.75.80
    port: 54379
    database: 0
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
  logpath: /Users/liuweiran/data/log
logging:
  file:
    name: ${spring.logpath}/${spring.application.name}-info.log
    path: ${spring.logpath}
  level:
    root: INFO
    org.springframework.boot.autoconfigure: ERROR
  pattern:
    file: '%d{yyyy-MM-dd HH:mm:ss} - %msg%n'
  logback:
    rollingpolicy:
      max-file-size: 500MB
      max-history: 7
      total-size-cap: 10GB
      clean-history-on-start: true
#微信校验信息
wx:
  mp:
    appId: "wx1a4ad02c7c500599"
    secret: "cf419db20b750aaa63007ed3209c9783"
    token: "0ADCEAF6CCE07DF3"
    aesKey: "kOQ1eQ4XnWl24C07XoNfjHiJ0oa8nxa20O97yqNlds6" # 加密模式需要
deepseek:
  apiKey: "sk-eca12b3a36df4f16a70df45f910f252d"
#是否开启跨域请求，开发环境在没有nginx代理的时候需要启动跨域请求
app:
  cors:
    enabled: true
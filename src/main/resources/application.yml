spring:
  application:
    name: waiter-Wechat
  profiles:
    active: '@spring.profiles.active@'
mybatis:
  mapper-locations: classpath:/mapper/*Mapper.xml
  type-aliases-package: xin.pwdkeeper.wechat.mapper